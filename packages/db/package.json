{
  "name": "@ai-edge/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate",
    "migrate:demo": "dotenv -e ../../.env.local -- drizzle-kit migrate",
    "studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@ai-edge/shared": "workspace:*",
    "@neondatabase/serverless": "^0.10.0",
    "drizzle-orm": "^0.36.0"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "drizzle-kit": "^0.28.0",
    "typescript": "^5.6.0"
  }
}
