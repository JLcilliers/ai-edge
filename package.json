{
  "name": "ai-edge",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "engines": { "node": ">=24.0.0" },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@vercel/config": "^0.2.0",
    "turbo": "^2.3.0",
    "typescript": "^5.6.0"
  }
}
